eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    config:
      enabled: true
  
# Configuration commune à tous les microservices (optionnelle)
spring:
  profiles:
    active: default
